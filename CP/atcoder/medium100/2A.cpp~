#include<bits/stdc++.h>
using namespace std;

int main() {
	string S;
	cin >> S;
	int ans = 0;
	int b = 0;
	for(int i=0; i<S.size(); i++) {
		if(S[i] == 'B') {
			++b;
		}else {
			ans += b;
		}
	}
	cout << ans << endl;
	return 0;
}
