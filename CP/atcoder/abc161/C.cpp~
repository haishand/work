#include<bits/stdc++.h>
using namespace std;

using ll = long long;

int main() {
	ll n, k;
	cin >> n >> k;
	if(n%k == 0) {
		cout << 0 << endl;
	}else {
		ll a;
		a = abs(n-k);
		while(a>abs(a-k)) {
			a = abs(a-k);
		}
		cout << a << endl;
	}
	return 0;
}
