#include<bits/stdc++.h>
using namespace std;

using ll = long long;

int main() {
	int n;
	cin >> n;

	ll ans = 0;
	if(n%2) {
		ans = 0;
	}else {
		ans = 1;
		for(int i=2; i<=n; i+=2) {
			ans *= 2;
		}
	}
	cout << ans << endl;

	return 0;
}
